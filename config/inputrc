# .inputrc -- needed by some programs to set readline opts (ex. gdb)

# Bug with untype-able 's' if next line enabled.
# Though Padraig says to use it:
# http://www.pixelbeat.org/settings/.inputrc
##source /etc/inputrc

set editing-mode vi
#set editing-mode emacs

#set bell-style visible

# Enable ALT in gnome-terminal, for vim tabbing.
# OOPS: it still does not work.
#set convert-meta on

# Enable 8-bit latin1. Big problem with any repl needing latin1 for umlauts
# etc. http://www.softpanorama.org/Scripting/Shellorama/inputrc.shtml
set convert-meta off

# Only require 1 tab for completion.
set show-all-if-ambiguous on

# Show filetype indicators.
set visible-stats on

set blink-matching-paren on

set history-size 10000

"\C-e": rlwrap-call-editor
