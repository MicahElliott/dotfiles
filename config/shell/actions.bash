######################################################################
# Actions (bash only)
######################################################################

# Activate prompt.
source ~/config/prompt/versatile
#versatile cyan

test -f /etc/bashrc           && source /etc/bashrc
test -f /etc/bash_completion  && source /etc/bash_completion

# This is already done for me by bash.
#test -f ~/.bash_completion    && source ~/.bash_completion
